px2rem($px) {
  $ratio = 375 / 10;
  return ($px/$ratio) rem;
}

center()
  display: flex
  justify-content: center
  align-items: center

left()
  display flex
  justify-content flex-start
  align-items center

right()
  display flex
  justify-content flex-end
  align-items center
